doctype html
html(lang="en", class="h-full w-full")
    head
        meta(charset="utf-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        link(rel=icon type="image/png" href="/img/cluster_favicon.png")
        meta(name="viewport", content="width=device-width, height=device-height, initial-scale=1, maximum-scale=1, shrink-to-fit=no")
        link(rel="stylesheet" href=assets.cssFile)

        title= title

    body
        section(id="root")
        script.
            window.base = Object.freeze(!{JSON.stringify(base)});

        script(defer=true type=module async=true src=assets.jsFile)
